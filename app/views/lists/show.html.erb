<div class="container">
  <h1><%= @list.name %></h1>
  <%= link_to "Add Bookmark",  new_list_bookmark_path(@list.id), class: "btn btn-primary mb-3" %>

  <div class="cards">
    <% @bookmarks.each do |bookmark| %>
      <div class="card">
        <%= image_tag bookmark.movie.poster_url, alt: "Poster", class: "card-img-top" %>
        <div class="card-body">
          <h5 class="card-title"><%= bookmark.movie.title %> - <%= bookmark.movie.rating %> ❤</h5>
           <p class="card-text"><%= bookmark.movie.overview %></p>
        </div>
      </div>
    <% end %>
  </div>
</div>
